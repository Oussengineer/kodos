{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && cd server && npm install && cd .. && npm run build:all"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "cd server && node index.js"
  }
}
